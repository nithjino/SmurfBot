pipeline {
    agent {label "default" }
    stages {
        stage('Install Depedencies') {
            steps {
                echo "install dependencies"
                sh 'sleep 10s'
            }
        }

        stage('Create Docker Image') {
            steps {
                echo "create docker image"
                sh 'sleep 10s'
            }
        }
    }
}